{
  "name": "future-crm-root",
  "version": "1.0.0",
  "description": "Root configuration for Mini CRM - Client Lead Management System",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "concurrently \"npm start --prefix backend\" \"npm run build --prefix frontend && npm start --prefix backend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
